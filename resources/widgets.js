import Widget from '../models/widget';

const Widgets = {
  resource: Widget,
  options: {
    properties: {
      _id: {
        isVisible: false,
      },
      i: {
        isVisible: false,
      },
    },
  },
};

export default Widgets;
